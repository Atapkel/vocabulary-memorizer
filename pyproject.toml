[project]
name = "spaced-repitition"
version = "0.1.0"
description = "Private Telegram vocabulary and lesson spaced repetition bot"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "python-dotenv>=1.2.3",
    "python-telegram-bot>=22.8",
]
